<div class="row">
  <div class="col-md-6 offset-md-3">
      <h1 class="text-center">ルーム一覧</h1>
      <% @rooms.each do |room| %>
        <%= link_to room.title, room_path(room) %>
      <% end %>
  </div>  
</div>